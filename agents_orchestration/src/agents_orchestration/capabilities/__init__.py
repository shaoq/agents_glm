"""Capability layer: capability descriptors, the capability registry and the
router that enforces worker allowlists, run/system policy, data scope,
availability and budget before any capability is invoked.
"""
