# RAG 知识点：知识库构建（Knowledge Base Construction）

> **文档定位**：本文是 RAG 技术知识点参考，聚焦**知识库构建（索引管线）**的原理、关键技术与最佳实践取舍。
>
> **与架构设计文档的区别**：
> - 本文档讲「为什么这样做、有哪些方案与取舍、最佳实践是什么」
> - [架构设计文档](../specs/2026-07-21-agents-rag-design.md) 讲「agents_rag 具体怎么做、选了什么、接口怎么定」
>
> **更新日期**：2026-07-21

---

## 0. 核心认知

知识库构建的本质，是把**「给人看的文档」转成「机器可高效检索、可溯源、低噪声的语义索引」**。在 RAG 体系里这条线叫 **索引管线（indexing pipeline）**，是离线 / 批处理过程。

三个关键判断：

1. **质量决定上限**：查询侧（检索、重排、生成）再精巧，也救不了一个构建得烂的知识库（garbage in, garbage out）。
2. **70% 法则**：工作量与质量风险，约 **70% 集中在「文档解析」和「分块」两步**；向量化反而是最标准化的一步。
3. **三段总纲**：
   - **解析**决定「信息能不能拿出来」
   - **分块**决定「信息能不能被精准召回」
   - **元数据**决定「能不能溯源回去」

三者缺一不可。

---

## 1. 全景：知识库构建的 8 个环节

```
采集接入 → 文档解析 → 清洗归一化 → 分块(Chunking) → 向量化索引
        → 元数据管理 → 持久化 → 增量维护
```

一条主线：从原始文档到可检索索引的完整流水线。下文逐环节展开。

---

## 2. 采集与接入（入口）

- **数据源**：本地文件 / 对象存储 / 数据库 / 网页爬取 / 内部 API
- **格式清单**：PDF、Office（Word/Excel/PPT）、HTML、Markdown、邮件、代码……
- **增量发现**（关键，否则只能全量重建）：
  - 文件指纹（内容 hash，如 SHA-256）
  - 修改时间（mtime）
  - 目录监听（watch）

> **agents_rag 落点**：扫描 `data/raw/`，对每个文件算 SHA-256 指纹，与已索引清单比对 → 未变更跳过、已删除清理、已更新重索引。

---

## 3. 文档解析（质量上限的瓶颈）⭐

知识库构建里**最难、最影响上限**的一步。解析不好，后面全是无米之炊。

### 3.1 为什么难

PDF 本质是「版面绘制指令」（文字框 + 坐标），**不是结构化文档**。典型难点：

- **多栏布局**：阅读顺序错乱（左栏读到右栏）
- **表格**：被拆成零散文字，行列关系丢失
- **图文混排**：图表说明、公式、题注错位
- **页眉页脚 / 水印**：污染正文
- **扫描件**：根本没有文字层，要 OCR

### 3.2 解析的四个层次

1. **版面分析（Layout Analysis）**：识别文字块、表格、图片、标题的区域
2. **阅读顺序还原**：多栏、跨页的正确顺序
3. **结构识别**：标题层级、段落、列表、表格单元
4. **内容提取 + OCR**：文字 + 扫描件识别

### 3.3 工具选型对比

| 工具 | 强项 | 弱项 |
|------|------|------|
| **Docling** | 输出结构化（标题/段落/表格带类型），表格识别强，输出 Markdown/JSON | 极复杂学术 PDF 偶有遗漏 |
| **MinerU** | 复杂中文/学术 PDF 极强（公式、双栏、表格），保留版面 | 依赖较重（带模型） |
| Unstructured | 全格式通用、生态广 | 表格一般，中文 PDF 不如前两者 |
| PyMuPDF / pdfplumber | 轻量、可控 | 需自己处理结构逻辑 |

### 3.4 关键设计原则

- **结构必须保留**：标题层级（H1/H2/H3）是后面「结构感知分块」的输入
- **位置必须保留**：页码、bbox——这是引用溯源的根基
- **表格必须整体化**：不能拆成散字，要保留行列结构（甚至可转 Markdown 表）

> **agents_rag 落点**：Docling 主力（结构化输出 + 表格）→ 复杂中文/学术 PDF 用 MinerU 兜底 → 文本过少判定为扫描件走 RapidOCR。`ParserRouter` 按扩展名 + 内容探测路由，失败自动降级。

---

## 4. 分块（Chunking）⭐⭐ —— 知识库构建的核心技术

如果说解析是「拿出来」，分块就是「切得对」。**这是知识库质量最关键的杠杆**。

### 4.1 为什么要分块

- **检索粒度**：整篇文档做向量，召回时信号被稀释（一个向量代表几万字，必然模糊）
- **上下文预算**：LLM 输入有限，要精准喂相关片段
- **召回精度**：块越小越精确，但太小会丢上下文 → **核心矛盾**

> 经验法则：
> - chunk 太大 → 噪声多、token 浪费、召回稀释
> - chunk 太小 → 语义残缺、答案缺上下文
>
> **没有银弹，要按数据调**。

### 4.2 分块策略（从简单到高级）

**① 固定大小分块**：按 token / 字符数硬切 + overlap。最简单，但会**从句子/表格中间切断**，基线可用、质量差。

**② 递归字符分块**：按分隔符优先级切（`\n\n` 段落 → `\n` → `。` 句 → 空格）。主流基线，比固定好，但仍忽略文档结构。

**③ 结构感知分块**（推荐）：利用解析得到的标题层级，**按「章节 / 小节 / 段落」自然边界切**。一块属于一个语义单元，质量最高。

**④ 语义分块**：用 embedding 相邻句相似度找「语义断点」。自适应，但要预计算 embedding，慢、成本高。

**⑤ 格式感知**：表格、列表、代码块**必须整体保留**，绝不在内部切断。常被忽略，但对表格密集的领域文档（财务、法规）是致命的。

### 4.3 进阶设计：检索粒度 ≠ 回传粒度（现代 RAG 的关键）⭐

分块最精髓之处。初学者用一个块既检索又回传，但**二者诉求不同**：

| | 诉求 | 粒度 |
|---|------|------|
| 检索 | 精准命中 | 小块 |
| 回传给 LLM | 上下文完整 | 大块 |

解决策略：

- **父子分块（Parent-Child / Small-to-Big）**：把文档切成大父块，父块再切成小子块；**用子块做 embedding 检索，命中后回传父块**。检索精准 + 上下文完整，两全。
- **句子窗口（Sentence Window）**：检索单句，回传时带前后 N 句。
- **摘要 + 细节双层**：文档 / 章节摘要建一层索引，命中后取对应细节。

> **agents_rag 落点**：**结构感知分块 + 父子分块**。Docling 输出的结构作为切分边界；子块用于 embedding 检索，父块（含完整段落 / 小节）回传给 GLM。每个块带 `parent_id`，查询时通过 parent_id 取回父块。

### 4.4 关键参数（要调，别照抄）

- `chunk_size`：中文约 300–600 字 / 英文 200–500 token
- `overlap`：10–20%（防止边界信息丢失）
- 最小 / 最大块约束（避免空块和巨型块）

### 4.5 元数据附着（每个 chunk 必带）

```
doc_id | source | page | heading | chunk_idx | parent_id | (可选) section_path
```

**没有元数据 = 无法引用溯源 = 企业场景不可用。**

---

## 5. 向量化与索引（最标准的一步）

- **模型**：embedding-3，默认 2048 维
- **维度权衡**：2048 最高精度（领域文档推荐）/ 1024 平衡 / 512 省
- **批处理**：单次 ≤ 64 条、单条 ≤ 3072 tokens
- **并发控制**：按账号等级限流（V0=50 … V3=500 在途），用信号量限流 + 指数退避
- **embedding 缓存**：相同文本不重复 embed（省成本、可复现）——常被忽略但价值大
- **双索引并存**：
  - 向量库（Chroma，HNSW 算法）→ 语义召回
  - BM25 索引（jieba 分词）→ 关键词 / 专有名词召回
  - 二者并存，查询时混合检索（RRF 融合）

> 常见误区：只建向量索引。纯向量对**型号、编号、人名、专有术语**召回差——这正是坚持**向量 + BM25 双索引**的理由。

---

## 6. 元数据与（可选）知识图谱

- **元数据**：来源、时间、作者、章节路径、页码、权限标签。是**过滤检索、引用溯源、权限控制**的基础。
- **知识图谱**（高级，非第一版目标）：从文档抽取实体 / 关系，支持结构化推理与多跳检索。复杂领域（医疗、法律）收益大，但工程重。

---

## 7. 持久化与增量维护（运维关键）

- **文档指纹表**：`doc_id(fingerprint) → 状态 / 索引位置`，持久化
- **增量 upsert**：只重算变更文档，不全量重建
- **删除同步**：源文件消失则清理其所有 chunk 与向量
- **版本 / 时间戳**：支持文档多版本与回溯

> **agents_rag 落点**：维护一份指纹清单文件，索引管线启动时 diff 出「新增 / 更新 / 删除」三类，分别处理。这是「先小后大」能扩到中规模的前提。

---

## 8. 知识库质量保障（最常被忽视）

知识库构建不是「跑一次就完」，要有评估闭环：

- **分块抽检**：人工看一批 chunk 是否语义完整、无截断
- **召回测试集**：准备一批 query + 「应被召回的文档 / chunk」，量化召回率（recall@k）
- **embedding 健康度**：相关 / 不相关对的相似度分布是否合理
- **参数迭代**：chunk_size、overlap、top_k 反复调
- 与查询侧的 **RAGAS 评估互补**：RAGAS 评「答得好不好」，召回测试评「找得准不准」

---

## 9. 常见陷阱清单（避坑）

| 陷阱 | 后果 |
|------|------|
| 一刀切固定大小分块 | 切坏句子 / 表格，召回残缺 |
| 忽略表格 / 结构 | 表格变乱码，领域数据丢失 |
| 不带元数据 | 无法引用溯源 |
| 只建向量索引 | 专有名词 / 编号召回差 |
| 不做增量 | 数据一变就全量重建，慢且贵 |
| embedding 无缓存 | 重复计算，成本高、不可复现 |
| 解析质量差 | 上限被锁死，下游再优化也无效 |

---

## 10. 小结：知识库构建配方（agents_rag）

```
data/raw/ (指纹 diff)
   → ParserRouter (Docling → MinerU → OCR 降级)
   → 清洗归一化
   → 结构感知分块 + 父子分块 (带 page/heading/parent_id)
   → embedding-3 批量向量化 (缓存 + 限流)
   → Chroma 向量库 + BM25 索引 (双索引)
   → 指纹清单持久化 (支持增量)
   → 召回测试集 + 抽检 (质量闭环)
```

核心取舍：**在「解析」和「分块（父子）」上投入最多**（质量杠杆）；向量和检索用成熟方案标准化。

---

## 11. 参考资料

- [智谱 Embedding-3 文档](https://docs.bigmodel.cn/cn/guide/models/embedding/embedding-3)
- [智谱 文本重排序（Rerank）](https://docs.bigmodel.cn/api-reference/%E6%A8%A1%E5%9E%8B-api/%E6%96%87%E6%9C%AC%E9%87%8D%E6%8E%92%E5%BA%8F)
- [Docling](https://github.com/docling-project/docling)
- [MinerU (PDF-Extract-Kit)](https://github.com/opendatalab/MinerU)
- 架构落地详见：[agents_rag 设计文档](../specs/2026-07-21-agents-rag-design.md)
